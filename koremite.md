# EncodeLab v1

PySide6 + ffmpeg ベースの動画変換ツールです。

## セットアップ

```powershell
git clone <repo>
cd encode-lab

python -m venv .venv
.\.venv\Scripts\Activate.ps1

pip install -r requirements.txt
pip install pyinstaller